export default function LessonProgress({ watched, completions, optional = false }: { watched: boolean; completions: number; optional?: boolean }) {
  return <section className="lesson-progress" aria-label="Your record for this lesson">
    <dl><div><dt>Video</dt><dd>{watched ? 'Watched' : 'Not marked watched'}<small>{optional ? 'Optional lesson · separate from core completion' : completions ? completions + ' recorded ' + (completions === 1 ? 'completion' : 'completions') : 'No new completion events recorded'}</small></dd></div></dl>
    <details><summary>How this record works</summary><p>{optional ? 'Finishing playback or choosing Mark video watched saves the watched status to your account. Undo removes the mark. Optional videos do not change required course completion.' : <>Opening a lesson does not complete it. Finishing playback or choosing “Mark video watched” records a completion; replaying can add another. Older watched marks are preserved without guessing how many times you watched. Undo changes the watched status, not the event history. Required course progress counts each watched required video once. These records are saved to your account, sync across devices and are included in your progress backup.</>}</p></details>
  </section>;
}
